# Days 8-11: Student training + QAT + INT8 TFLite export.
# TF 2.15 ships with cuDNN 8.9 / CUDA 12.2 compatibility, matches system modules.
# tensorflow-model-optimization 0.8 is the last release compatible with TF 2.15.
# Install with:
#   pip install -r requirements_tf.txt

tensorflow[and-cuda]==2.15.1
tensorflow-model-optimization==0.8.0
tf-keras==2.15.1

# Image pipeline (same as torch env for cross-framework data prep)
opencv-python-headless>=4.9.0
Pillow>=10.2.0
einops>=0.7.0

# Utilities
numpy<2.0
scipy>=1.11
scikit-learn>=1.3
pandas>=2.0
matplotlib>=3.7
seaborn>=0.13
tqdm>=4.66
tensorboard>=2.15
PyYAML>=6.0
omegaconf>=2.3
hydra-core>=1.3

# Dataset download (also useful here for re-downloading or split-only)
kaggle>=1.6.0

# Notebook
jupyterlab>=4.0
ipywidgets>=8.0
